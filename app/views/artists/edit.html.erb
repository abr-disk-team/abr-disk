<!-- ヘッダー -->
<% if user_signed_in? && current_user.admin %>
    <%= render 'template/admin_header' %>
<% elsif user_signed_in? %>
    <%= render 'template/user_header' %>
<% else %>
    <%= render 'template/visiter_header' %>
<% end %>

EDIT ARTIST

<%= form_for(@artist) do |f| %>
    <%= f.text_field :artist %>
    <%= f.submit %>

    <%= link_to "Destroy",@artist, method: :delete, data: {confirm: "本当に削除してもよろしいですか？"}, class: "btn-sm btn-danger destroy_artist_#{@artist.id}" %>

<% end %>

<!-- フッター -->
<%= render 'template/footer' %>