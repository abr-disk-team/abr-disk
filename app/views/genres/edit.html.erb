<!-- ヘッダー -->
<% if user_signed_in? && current_user.admin %>
    <%= render 'template/admin_header' %>
<% elsif user_signed_in? %>
    <%= render 'template/user_header' %>
<% else %>
    <%= render 'template/visiter_header' %>
<% end %>

EDIT GENRE


<%= form_for(@genre) do |f| %>
    <%= f.text_field :genre %>
    <%= f.submit %>

    <%= link_to "Destroy",@genre, method: :delete, data: {confirm: "本当に削除してもよろしいですか？"}, class: "btn-sm btn-danger destroy_genre_#{@genre.id}" %>
<% end %>

<!-- フッター -->
<%= render 'template/footer' %>